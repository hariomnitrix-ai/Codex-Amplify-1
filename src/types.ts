export type Lifecycle='discovered'|'needs_review'|'approved'|'generated'|'tested'|'staged'|'publish_ready'|'published'|'paused';
export type Risk='low'|'medium'|'high'|'unknown';
export interface Evidence { source:string; observedAt:string; value:number; note?:string }
export interface Candidate { id:string; title:string; category:string; queries:string[]; buyer:string; demand:Evidence[]; competitors:number; medianCompetitorQuality:number; estimatedValuePerThousandUsd:number; implementationDays:number; publicOrPermitted:boolean; tosReviewed:boolean; robotsReviewed:boolean; personalData:boolean; durability:number; risk:Risk; lifecycle:Lifecycle; notes?:string[] }
export interface ScoreBreakdown { demand:number; competition:number; buyerValue:number; feasibility:number; durability:number; riskPenalty:number; total:number; decision:'reject'|'review'|'build' }
export interface PricingEvent { name:string; title:string; priceUsd:number; unit:string; description:string }
export interface TargetConfig { schemaVersion:1; id:string; title:string; description:string; source:{kind:'json-api'|'html';baseUrl:string;allowedHosts:string[];publicOrPermitted:boolean;termsUrl:string;robotsUrl?:string;reviewedAt:string}; request:{startUrls:string[];method:'GET'|'POST';body?:unknown;rateLimitPerMinute:number;maxConcurrency:number;headers?:Record<string,string>}; extraction:{itemSelector?:string;fields:Record<string,string>;identityFields:string[];nextPageSelector?:string}; output:{maxItemsDefault:number;maxItemsHard:number}; pricing:{events:PricingEvent[]}; seo:{keywords:string[];buyer:string}; compliance:{personalData:boolean;licenseNotes:string;retentionDays:number}; fixtures:string[] }
export interface Approval { candidateId:string; actorId:string; action:'publish-paid';approvedBy:string;issuedAt:string;expiresAt:string;configHash:string;signature:string }
