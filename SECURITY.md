# Security

Report vulnerabilities privately to the repository owner. Never commit Apify tokens, approval secrets, cookies, proxy credentials, or exported personal data. Publication approvals are narrow, expiring HMAC records bound to the exact configuration hash. CI has read-only content permission by default.
